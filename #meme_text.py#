input_text = input("Text to be meme-ified B)")
input_text_arr = list(input_text)
new_arr = []
for x, y in enumerate(input_text_arr):
    if x == len(input_text_arr):
        break
    elif y != ' ':
        new_arr.append(y)
        new_arr.

print(memetext())
