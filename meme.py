class NODES_ARE_REALLY_GOOD:
        def __init__(self, val, prior=None, next=None):
            self.val = val
            self.prior = prior
            self.next = next

        def FUCK_MY_SHIT_UP(self, NEW_VALUE_FOR_THE_NEW_NODE):
            # makes the next node something i dont know
            self.next.prior.next.prior.next.prior.next.prior.next.prior.next.prior.next = NODES_ARE_REALLY_GOOD(NEW_VALUE_FOR_THE_NEW_NODE)
            STRING_THAT_WILL_BE_RETURNED = "D O      Y O U        B U Y         T H I S    ? ? ? ?  ? ? ? ? ? "
            return STRING_THAT_WILL_BE_RETURNED
